# Runtime processing report

DUN created the runtime GLB as original geometry, so the original and runtime SHA-256 are identical. The model is a quiet four-legged standing pose with two long upper canines and a short tail. There is no animation, camera, light, external URI, script, logo, text, or watermark node.

Khronos glTF Validator reports 0 errors and 0 warnings. Runtime metrics: 13312 bytes, 228 triangles, 1 draw call, no textures, and no animation.

Runtime SHA-256: `c8b558967f631818018aa974129f311c0bae53e0c32d971084cd945ec8be1f9f`.
